# Point the Vite app at TravelPlanner-dev (stack ApiEndpoint = Function URL, no trailing slash)
VITE_API_BASE_URL=https://xxxxxxxx.lambda-url.us-west-2.on.aws

# Clerk publishable key must match the CLERK_ISSUER configured on that stack
VITE_CLERK_PUBLISHABLE_KEY=pk_test_...
